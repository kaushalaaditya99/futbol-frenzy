import { View } from "react-native";

// Small resuable components that don't need their own file

export function Divider() {
    return <View style={{ height: 1, backgroundColor: "#F0F0F0", marginLeft: 46 }} />;
}